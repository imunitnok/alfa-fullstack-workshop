using System;

namespace Server.Infrastructure
{
    /// <summary>
    /// Enum for card types
    /// </summary>
    public enum CardType
    {
        UNKNOWN = 0,
        MASTERCARD = 1,
        VISA = 4,
        MAESTRO = 5,
        MIR = 6
    }
}